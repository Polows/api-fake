{
  "name": "api-fake",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-api": "json-server --watch ./db/database.json --port 3004",
    "start-api-js": "json-server script/employees.js",
    "generate-data": "node script/generateData",
    "prestart-mockapi": "npm run generate-data",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-schema-faker": "^0.4.6",
    "json-server": "^0.12.0"
  }
}
